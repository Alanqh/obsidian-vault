{
  "name": "obsidian-vault-docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docs:dev": "node scripts/prepare-docs.mjs && vitepress dev docs",
    "docs:build": "node scripts/prepare-docs.mjs && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
